apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "predbat.fullname" . }}-test-connection"
  labels:
    {{- include "predbat.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "predbat.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
